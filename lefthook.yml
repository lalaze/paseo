pre-commit:
  parallel: true
  jobs:
    - name: format
      glob: "*.{css,js,json,jsonc,jsx,md,mjs,ts,tsx,yaml,yml}"
      exclude:
        - "package-lock.json"
        - "**/package-lock.json"
      run: npm run format:check:files -- {staged_files}
    - name: lint
      glob: "*.{js,jsx,mjs,ts,tsx}"
      exclude:
        - "scripts/personal-quota/providers/**"
      run: npm run lint -- {staged_files}
    - name: typecheck
      run: npm run typecheck
